import React from 'react';

const Button = ({button}) => {
    return (
        <button className='bg-yellow-light px-10 py-3  font-sen text-dark-blue font-bold'>{button}</button>
    );
};

export default Button;